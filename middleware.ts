import {createServerClient} from "@supabase/ssr"; import {NextResponse,type NextRequest} from "next/server";
export async function middleware(req:NextRequest){let res=NextResponse.next({request:{headers:req.headers}});const s=createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,{cookies:{getAll:()=>req.cookies.getAll(),setAll(a){a.forEach(({name,value})=>req.cookies.set(name,value));res=NextResponse.next({request:{headers:req.headers}});a.forEach(({name,value,options})=>res.cookies.set(name,value,options))}}});const {data:{user}}=await s.auth.getUser();if(req.nextUrl.pathname.startsWith("/admin")&&!req.nextUrl.pathname.startsWith("/admin/login")&&!user)return NextResponse.redirect(new URL("/admin/login",req.url));if(req.nextUrl.pathname==="/admin/login"&&user)return NextResponse.redirect(new URL("/admin/dashboard",req.url));return res}export const config={matcher:["/admin/:path*"]};
