"use client"; import {useEffect,useState} from "react"; import {Trash2} from "lucide-react"; import {supabaseBrowser} from "@/lib/supabase/client"; import {PageHeader} from "@/components/PageHeader";
export default function Drafts(){const db=supabaseBrowser();const [rows,setRows]=useState<any[]>([]);async function load(){const {data}=await db.from('messages').select('*').eq('status','draft').order('created_at',{ascending:false});setRows(data??[])}useEffect(()=>{load()},[]);async function del(id:string){if(confirm('Delete draft?')){await db.from('messages').delete().eq('id',id);load()}}return <><PageHeader title="Drafts" description="Saved messages waiting to be completed."/><div className="space-y-3">{rows.map(r=><div className="card p-5 flex justify-between gap-4" key={r.id}><div><b>{r.body||'Empty draft'}</b><div className="mt-1 text-xs text-[#746f66]">{new Date(r.created_at).toLocaleString()}</div></div><button className="text-red-600" onClick={()=>del(r.id)}><Trash2 size={17}/></button></div>)}{!rows.length&&<div className="card p-10 text-center text-sm text-[#746f66]">No drafts yet.</div>}</div></>}
